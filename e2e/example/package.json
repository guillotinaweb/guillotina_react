{
  "name": "nextjs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "next",
    "build": "cd ../../package && yarn && yarn build && yarn build:css && cd ../e2e/example && yarn",
    "start": "cd ../../package && yarn && yarn build && yarn build:css && cd ../e2e/example && yarn && yarn dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@zeit/next-css": "^1.0.1",
    "next": "^10.0.3",
    "react-gmi": "link:../.."
  }
}
